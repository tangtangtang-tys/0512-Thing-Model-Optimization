export interface RequirementDoc {
  id: string;
  title: string;
  sourceName: string;
  content: string;
}

export const requirementDocs: RequirementDoc[] = [
  {
    id: 'overview',
    title: '功能需求梳理',
    sourceName: '物模型功能需求梳理.md',
    content: `# 物模型当前创建流程

选择功能类目 -> 创建产品类别 -> 创建功能项 -> 选择对应功能项 -> 添加物模型（属性/服务/事件） -> 完成该功能项的物模型创建 -> 完成发布该功能项。

# 现存问题

每次更新功能项中的物模型信息时都需要重新创建功能项并且重新设置物模型定义，增加用户操作成本，流程繁琐，同时无法配置每个物模型的默认值信息。

# 需求目标

需要能针对不同产品类目下的物模型方便管理及更新操作，需要支持配置物模型的定义的默认值。

当前整体的流程是有问题的，同时产品设计上考虑的方向也不够全面，需要针对以上问题以及标准的物联网平台的产品设计的标准进行流程优化，以专业的物联网产品专家的角度去评估判断指出当前平台该模块的问题，需要严格辩证指出不足，同时给出相对应的完整闭环流程梳理及架构。`
  },
  {
    id: 'analysis',
    title: '默认值优化需求分析',
    sourceName: '物模型默认值优化需求分析.md',
    content: `# 物模型默认值优化需求分析

## 1. 背景说明

当前平台的物模型创建链路为：

选择功能类目 -> 创建产品类别 -> 创建功能项 -> 进入功能项详情 -> 添加物模型（属性/服务/事件）-> 完成功能项物模型创建 -> 发布功能项。

从现有截图与需求描述看，当前系统将“功能项”作为物模型的主要承载对象。用户需要先维护产品类目和功能项，再在功能项详情中维护属性、服务、事件等物模型定义。该设计可以完成基础录入，但在物联网平台的标准产品能力视角下，存在物模型管理能力弱、复用能力弱、变更闭环不足、默认值配置缺失等问题。

本次优化目标不是简单增加一个“默认值”字段，而是需要围绕“产品类目下的物模型定义如何被标准化管理、复用、变更、发布和应用”进行整体流程重构。

## 2. 现状功能梳理

### 2.1 当前页面结构

当前模块主要包含以下页面与操作：

1. 功能类目列表
   - 左侧为产品类列表。
   - 右侧展示当前产品类下的功能项卡片。
   - 支持新增产品类、编辑产品类、新增功能项、搜索功能项。

2. 新增产品类
   - 支持填写类目名称、备注。
   - 类目更像功能分组，而不是严格意义上的产品品类模型。

3. 新增功能项
   - 支持填写功能项名称。
   - 支持关联硬件、关联类目。
   - 支持填写备注、上传功能示例图。

4. 功能项详情
   - 展示功能项基础信息。
   - 包含“物模型信息”和“关联硬件”两个页签。
   - 支持添加物模型、文本导入 JSON、编辑物模型、删除物模型、立即发布。

5. 添加/编辑物模型
   - 支持物模型名称、标识符、物模型类型、数据类型、数据定义、访问权限、备注。
   - 物模型类型包含属性、服务、事件。
   - 数据类型包含布尔、枚举、数值、数组、结构体等。
   - 复杂类型支持结构体参数、数组元素类型、元素个数等配置。

### 2.2 当前状态管理

截图中功能项存在“待发布”“未发布”等状态，并提供“立即发布”按钮。但当前发布对象主要体现为功能项，而不是物模型版本。物模型定义发生变更后，缺少清晰的草稿、变更差异、版本号、影响范围、发布审批、回滚机制。

## 3. 现存问题诊断

### 3.1 信息架构问题：功能项与物模型边界不清

当前系统将物模型维护放在功能项详情中，导致用户认知上容易将“功能项”等同于“物模型集合”。但在标准 IoT 平台中，物模型通常是产品能力的结构化定义，应该围绕产品品类、产品模板、设备型号或产品实例来管理。

当前设计存在以下问题：

1. 功能项承担了过多职责
   - 既是业务功能展示单元。
   - 又是物模型定义容器。
   - 又是发布对象。
   - 又承载硬件关联关系。

2. 产品类目能力不足
   - 产品类目仅作为左侧分组存在。
   - 缺少产品类目级物模型模板。
   - 缺少同类产品之间的能力继承、复用和差异化配置。

3. 物模型缺少独立管理视角
   - 物模型无法作为可复用资产被管理。
   - 缺少物模型库、模板库、版本库。
   - 不利于跨产品类目、跨硬件型号复用。

### 3.2 流程问题：更新成本过高

当前需求描述中提到：每次更新功能项中的物模型信息时，都需要重新创建功能项并重新设置物模型定义。

该流程会带来明显问题：

1. 重复配置成本高
   - 同一类物模型需要在多个功能项中重复录入。
   - 枚举值、访问权限、备注、数据定义等配置容易出现不一致。

2. 变更不可控
   - 用户无法清楚知道本次修改影响哪些功能项、产品类目、硬件型号或设备。
   - 缺少变更对比与影响范围提示。

3. 发布路径不完整
   - 修改物模型后缺少“草稿 -> 校验 -> 发布 -> 生效 -> 回滚”的闭环。
   - 发布动作过于粗粒度，无法支撑企业级 IoT 平台的稳定性要求。

### 3.3 默认值能力缺失

当前添加/编辑物模型表单中未体现默认值配置。对于 IoT 场景，默认值并不是一个简单字段，而应根据不同物模型类型、数据类型和生效场景分别设计。

缺失默认值会造成：

1. 设备初始化缺少标准配置
   - 新设备激活后无法自动获得产品建议配置。
   - 用户需要在设备侧或应用侧重复设置初始值。

2. App/云端展示缺少兜底值
   - 设备未上报属性时，应用无法明确显示默认状态。
   - 容易出现空值、异常值、展示不一致。

3. 批量配置效率低
   - 同品类设备无法快速套用默认参数。
   - 对枚举、开关、阈值、灵敏度、检测周期等配置项尤其低效。

4. 产品模板无法沉淀
   - 没有默认值，物模型定义只是字段结构，而不是可直接用于设备初始化和业务配置的完整模板。

### 3.4 版本与发布问题

当前“立即发布”操作作用在功能项上，但缺少以下关键能力：

1. 物模型版本号
   - 无法区分当前线上版本、草稿版本、历史版本。

2. 差异对比
   - 发布前无法看到新增、修改、删除了哪些物模型。

3. 影响范围评估
   - 缺少对硬件型号、设备数量、App 展示、云端规则、数据统计口径的影响提示。

4. 发布后回滚
   - 线上版本出现问题后，无法快速恢复到历史稳定版本。

## 4. 优化目标

### 4.1 产品目标

1. 建立产品类目级物模型模板。
2. 支持物模型定义与默认值统一维护。
3. 支持不同硬件型号在类目默认值基础上做覆盖。
4. 支持物模型草稿、版本、发布、回滚闭环。
5. 支持新设备初始化时按模板生成默认值快照。
6. 降低重复配置成本，提高跨类目、跨硬件复用能力。

### 4.2 用户目标

1. 运营或产品人员可以快速维护某个产品类目的标准物模型。
2. 硬件或研发人员可以明确看到字段定义、默认值、覆盖规则和发布状态。
3. 测试人员可以基于版本差异和默认值快照验证设备初始化效果。
4. 研发人员可以通过模板版本和 JSON 导入导出与后端接口联调。

## 5. 建议的信息架构

建议将模块拆分为以下层级：

1. 产品类目
   - 作为业务品类或产品族的一级容器。
   - 管理类目基础信息、适用硬件、当前模板版本。

2. 物模型模板
   - 作为产品类目下的标准能力定义集合。
   - 包含属性、服务、事件。
   - 支持草稿版本和线上版本。

3. 功能分组 / 功能项
   - 作为物模型展示和业务归类维度。
   - 不再作为唯一发布对象。
   - 一个功能项可以关联多个物模型。

4. 默认值策略
   - 挂载在具体物模型字段上。
   - 支持类目默认值、硬件覆盖值、设备初始化值。

5. 硬件覆盖
   - 不同硬件型号可基于类目模板覆盖部分默认值。
   - 覆盖必须经过数据类型、枚举范围、数值范围校验。

6. 模板版本
   - 每次发布生成不可变版本。
   - 支持差异查看、影响范围、回滚。

## 6. 建议的完整闭环流程

### 6.1 创建产品类目

用户先创建产品类目，例如“宠物饮水机”“宠物喂食器”“IPC 摄像机”等。

类目字段建议包含：

1. 类目名称
2. 类目编码
3. 适用产品线
4. 关联硬件型号
5. 描述
6. 当前模板版本
7. 发布状态

### 6.2 创建或导入物模型模板

在产品类目详情中维护物模型模板：

1. 支持手动新增属性、服务、事件。
2. 支持 JSON 导入。
3. 支持从模板库复制。
4. 支持从历史版本复制。

物模型字段建议包含：

1. 名称
2. 标识符
3. 类型：属性 / 服务 / 事件
4. 数据类型
5. 数据定义
6. 访问权限
7. 功能分组
8. 是否必填
9. 是否参与设备初始化
10. 默认值策略
11. 备注
12. 状态

### 6.3 配置默认值

默认值配置应按数据类型提供不同表单：

1. 布尔
   - 默认开启 / 关闭
   - 展示文案

2. 枚举
   - 默认枚举项
   - 枚举项展示文案

3. 数值
   - 默认值
   - 单位
   - 最小值 / 最大值 / 步长

4. 字符串
   - 默认字符串
   - 最大长度
   - 格式校验

5. 数组
   - 默认数组
   - 元素类型
   - 元素数量限制

6. 结构体
   - 子字段默认值
   - 子字段校验规则

默认值策略建议支持：

1. 是否启用默认值
2. 默认值来源：类目模板 / 硬件覆盖 / 设备首次激活 / 用户设置继承
3. 是否写入设备初始化快照
4. 是否允许硬件型号覆盖
5. 是否允许用户侧修改
6. 生效时机：创建设备 / 首次激活 / 模板升级 / 手动同步

### 6.4 发布前校验

发布前建议增加校验清单：

1. 标识符唯一性校验
2. 数据类型合法性校验
3. 默认值是否符合数据定义
4. 枚举默认值是否属于枚举范围
5. 数值默认值是否在最小值和最大值之间
6. 必填字段是否完整
7. 硬件覆盖是否存在冲突
8. 删除字段是否影响线上设备

### 6.5 发布模板版本

发布时生成新模板版本，例如：

- v1.0.0
- v1.1.0
- v2.0.0

发布弹窗需要展示：

1. 变更摘要
2. 新增、修改、删除的物模型列表
3. 默认值变更列表
4. 影响硬件型号
5. 影响设备数量
6. 发布说明
7. 回滚风险提示

### 6.6 设备初始化与生效

设备首次绑定、激活或升级模板时，系统根据以下优先级计算默认值：

1. 设备用户配置值
2. 硬件型号覆盖值
3. 产品类目模板默认值
4. 系统兜底值

最终结果写入设备默认值快照，用于：

1. App 初始展示
2. 云端规则计算
3. 设备下发配置
4. 版本追踪和问题排查

## 7. 页面设计建议

### 7.1 产品类目列表页

建议保留左侧产品类树或列表，但右侧不应只展示功能项卡片，而应增加：

1. 当前模板版本
2. 发布状态
3. 物模型数量
4. 已配置默认值数量
5. 草稿变更数量
6. 关联硬件数量
7. 最近发布时间

### 7.2 产品类目详情页

建议包含以下页签：

1. 物模型模板
2. 默认值配置
3. 硬件覆盖
4. 设备初始化效果
5. 版本记录
6. 审计日志

### 7.3 物模型编辑抽屉

建议在原有字段基础上增加“默认值配置”区域：

1. 是否启用默认值
2. 默认值输入控件
3. 默认值校验提示
4. 是否写入设备初始化
5. 是否允许硬件覆盖
6. 生效说明

### 7.4 发布弹窗

建议增加完整发布检查：

1. 变更对比
2. 默认值合法性校验
3. 硬件覆盖冲突
4. 影响范围
5. 发布说明

## 8. 关键业务规则

1. 标识符在同一产品类目模板内必须唯一。
2. 默认值必须符合数据类型和数据定义。
3. 未配置默认值的非必填字段可以为空，但需要明确展示为空策略。
4. 硬件覆盖值不能突破物模型的数据定义范围。
5. 已发布版本不可直接修改，只能基于版本创建草稿。
6. 删除物模型需要提示影响范围。
7. 设备已存在用户配置时，模板默认值升级不能直接覆盖用户设置。
8. 每次发布必须生成版本记录和审计日志。

## 9. 优先级建议

### P0

1. 物模型默认值字段与表单配置。
2. 默认值合法性校验。
3. 草稿与发布状态。
4. 发布前变更摘要。

### P1

1. 硬件型号默认值覆盖。
2. 设备初始化默认值快照。
3. 模板版本记录。
4. JSON 导入导出支持默认值。

### P2

1. 模板库复用。
2. 跨产品类目复制。
3. 影响范围分析。
4. 版本回滚。
5. 审计日志。

## 10. 建议的研发落地拆分

### 10.1 数据模型

需要新增或调整：

1. 产品类目表
2. 物模型模板表
3. 物模型定义表
4. 物模型默认值配置表
5. 硬件覆盖配置表
6. 模板版本表
7. 设备默认值快照表
8. 操作日志表

### 10.2 API

建议提供：

1. 查询产品类目模板
2. 新增 / 编辑 / 删除物模型
3. 配置物模型默认值
4. 校验默认值
5. 查询草稿变更
6. 发布模板版本
7. 查询模板版本
8. 回滚模板版本
9. 配置硬件覆盖
10. 计算设备初始化默认值

### 10.3 前端

需要实现：

1. 类目列表与详情。
2. 物模型表格。
3. 物模型编辑抽屉。
4. 默认值动态表单。
5. 发布确认弹窗。
6. 硬件覆盖页。
7. 设备初始化预览。
8. 版本记录。
9. 审计日志。

## 11. 总结

当前模块最大的问题不是缺少一个默认值输入框，而是产品信息架构和发布闭环不完整。

建议将物模型从“功能项详情里的配置项”提升为“产品类目模板的核心资产”，围绕模板、默认值、硬件覆盖、版本发布、设备初始化建立完整闭环。

这样才能真正降低配置成本、提升复用能力，并支撑后续多产品线、多硬件型号、多设备规模化管理。`
  }
];
