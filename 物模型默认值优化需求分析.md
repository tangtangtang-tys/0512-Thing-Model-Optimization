# 物模型默认值优化需求分析

## 1. 背景说明

当前平台的物模型创建链路为：

选择功能类目 -> 创建产品类别 -> 创建功能项 -> 进入功能项详情 -> 添加物模型（属性/服务/事件）-> 完成功能项物模型创建 -> 发布功能项。

从现有截图与需求描述看，当前系统将“功能项”作为物模型的主要承载对象。用户需要先维护产品类目和功能项，再在功能项详情中维护属性、服务、事件等物模型定义。该设计可以完成基础录入，但在物联网平台的标准产品能力视角下，存在物模型管理能力弱、复用能力弱、变更闭环不足、默认值配置缺失等问题。

本次优化目标不是简单增加一个“默认值”字段，而是需要围绕“产品类目下的物模型定义如何被标准化管理、复用、变更、发布和应用”进行整体流程重构。

## 2. 现状功能梳理

### 2.1 当前页面结构

当前模块主要包含以下页面与操作：

1. 功能类目列表
   - 左侧为产品类列表。
   - 右侧展示当前产品类下的功能项卡片。
   - 支持新增产品类、编辑产品类、新增功能项、搜索功能项。

2. 新增产品类
   - 支持填写类目名称、备注。
   - 类目更像功能分组，而不是严格意义上的产品品类模型。

3. 新增功能项
   - 支持填写功能项名称。
   - 支持关联硬件、关联类目。
   - 支持填写备注、上传功能示例图。

4. 功能项详情
   - 展示功能项基础信息。
   - 包含“物模型信息”和“关联硬件”两个页签。
   - 支持添加物模型、文本导入 JSON、编辑物模型、删除物模型、立即发布。

5. 添加/编辑物模型
   - 支持物模型名称、标识符、物模型类型、数据类型、数据定义、访问权限、备注。
   - 物模型类型包含属性、服务、事件。
   - 数据类型包含布尔、枚举、数值、数组、结构体等。
   - 复杂类型支持结构体参数、数组元素类型、元素个数等配置。

### 2.2 当前状态管理

截图中功能项存在“待发布”“未发布”等状态，并提供“立即发布”按钮。但当前发布对象主要体现为功能项，而不是物模型版本。物模型定义发生变更后，缺少清晰的草稿、变更差异、版本号、影响范围、发布审批、回滚机制。

## 3. 现存问题诊断

### 3.1 信息架构问题：功能项与物模型边界不清

当前系统将物模型维护放在功能项详情中，导致用户认知上容易将“功能项”等同于“物模型集合”。但在标准 IoT 平台中，物模型通常是产品能力的结构化定义，应该围绕产品品类、产品模板、设备型号或产品实例来管理。

当前设计存在以下问题：

1. 功能项承担了过多职责
   - 既是业务功能展示单元。
   - 又是物模型定义容器。
   - 又是发布对象。
   - 又承载硬件关联关系。

2. 产品类目能力不足
   - 产品类目仅作为左侧分组存在。
   - 缺少产品类目级物模型模板。
   - 缺少同类产品之间的能力继承、复用和差异化配置。

3. 物模型缺少独立管理视角
   - 物模型无法作为可复用资产被管理。
   - 缺少物模型库、模板库、版本库。
   - 不利于跨产品类目、跨硬件型号复用。

### 3.2 流程问题：更新成本过高

当前需求描述中提到：每次更新功能项中的物模型信息时，都需要重新创建功能项并重新设置物模型定义。

该流程会带来明显问题：

1. 重复配置成本高
   - 同一类物模型需要在多个功能项中重复录入。
   - 枚举值、访问权限、备注、数据定义等配置容易出现不一致。

2. 变更不可控
   - 用户无法清楚知道本次修改影响哪些功能项、产品类目、硬件型号或设备。
   - 缺少变更对比与影响范围提示。

3. 发布路径不完整
   - 修改物模型后缺少“草稿 -> 校验 -> 发布 -> 生效 -> 回滚”的闭环。
   - 发布动作过于粗粒度，无法支撑企业级 IoT 平台的稳定性要求。

### 3.3 默认值能力缺失

当前添加/编辑物模型表单中未体现默认值配置。对于 IoT 场景，默认值并不是一个简单字段，而应根据不同物模型类型、数据类型和生效场景分别设计。

缺失默认值会造成：

1. 设备初始化缺少标准配置
   - 新设备激活后无法自动获得产品建议配置。
   - 用户需要在设备侧或应用侧重复设置初始值。

2. App/云端展示缺少兜底值
   - 设备未上报属性时，应用无法明确显示默认状态。
   - 容易出现空值、异常值、展示不一致。

3. 批量配置效率低
   - 同品类设备无法快速套用默认参数。
   - 对枚举、开关、阈值、灵敏度、检测周期等配置项尤其低效。

4. 产品模板无法沉淀
   - 没有默认值，物模型定义只是字段结构，而不是可直接用于设备初始化和业务配置的完整模板。

### 3.4 版本与发布问题

当前“立即发布”操作作用在功能项上，但缺少以下关键能力：

1. 物模型版本号
   - 无法区分当前线上版本、草稿版本、历史版本。

2. 差异对比
   - 发布前无法看到新增、修改、删除了哪些物模型。

3. 兼容性校验
   - 删除字段、修改标识符、修改数据类型等高风险操作未被显式拦截。

4. 回滚机制
   - 发布错误后缺少回滚到上一稳定版本的入口。

5. 生效范围
   - 无法选择发布到全部产品、部分硬件型号、部分设备分组或仅保存模板。

### 3.5 数据建模问题

当前列表中的字段能够描述基础 TSL，但缺少面向平台化管理所需的元数据：

1. 缺少默认值字段。
2. 缺少是否必填、是否可继承、是否允许覆盖等模板控制字段。
3. 缺少版本字段。
4. 缺少来源字段，例如系统标准物模型、类目模板、自定义扩展、硬件继承。
5. 缺少兼容性策略，例如可新增、可废弃、禁止修改标识符。
6. 缺少发布状态字段，例如草稿、待发布、已发布、已废弃。

## 4. 设计目标

### 4.1 业务目标

1. 支持按产品类目统一管理物模型定义。
2. 支持物模型默认值配置，并可用于设备初始化、云端展示和批量配置。
3. 降低用户更新物模型的操作成本。
4. 建立物模型草稿、校验、发布、版本、回滚闭环。
5. 支持功能项、产品类目、硬件型号、设备实例之间的清晰关系。
6. 支持物模型复用，避免重复创建功能项和重复定义物模型。

### 4.2 产品目标

1. 将“物模型”从功能项附属配置提升为核心产品能力。
2. 将“功能项发布”优化为“物模型版本发布 + 功能项展示发布”的组合。
3. 将“新增物模型字段”升级为“产品类目物模型模板配置”。
4. 将默认值能力纳入物模型定义的完整数据结构。

## 5. 推荐信息架构

建议将当前模块调整为以下层级：

```text
产品类目
  -> 产品能力模板
      -> 功能项分组
      -> 物模型定义
          -> 属性
          -> 服务
          -> 事件
      -> 默认值策略
      -> 版本管理
  -> 硬件型号适配
      -> 继承类目模板
      -> 覆盖部分默认值
      -> 扩展硬件专属物模型
  -> 发布记录
      -> 草稿版本
      -> 已发布版本
      -> 历史版本
```

### 5.1 产品类目

产品类目用于承载同类产品的标准能力模板，例如婴儿看护器、IPC、宠物设备等。产品类目不应只是左侧列表分组，而应成为物模型模板的归属主体。

核心字段建议：

| 字段 | 说明 |
| --- | --- |
| 类目名称 | 产品品类名称 |
| 类目编码 | 全局唯一编码 |
| 类目说明 | 描述该品类适用范围 |
| 状态 | 草稿、启用、停用 |
| 当前模板版本 | 当前已发布的物模型模板版本 |
| 创建时间/更新时间 | 审计字段 |

### 5.2 功能项

功能项用于业务能力展示和前端组织，不应成为物模型定义的唯一容器。

建议定位：

1. 功能项是物模型的业务分组。
2. 一个功能项可以绑定多个物模型。
3. 一个物模型可以被多个功能项引用。
4. 功能项可以配置展示名称、图标、排序、说明、关联硬件范围。
5. 功能项本身不直接决定物模型字段的生命周期。

### 5.3 物模型定义

物模型定义应作为核心对象独立管理。

核心字段建议：

| 字段 | 说明 |
| --- | --- |
| 物模型名称 | 中文名称 |
| 标识符 | 英文唯一标识，同一模板版本内唯一 |
| 类型 | 属性、服务、事件 |
| 数据类型 | Bool、Enum、Int、Float、Double、Text、Date、Array、Struct |
| 数据定义 | 取值范围、步长、单位、枚举项、结构体参数等 |
| 访问权限 | 只读、读写、只写 |
| 默认值 | 当前物模型的默认值 |
| 默认值来源 | 类目默认、硬件覆盖、设备覆盖、系统默认 |
| 是否必填 | 设备初始化或模板发布时是否必须配置 |
| 是否允许覆盖 | 硬件型号或设备实例是否可覆盖默认值 |
| 状态 | 草稿、已发布、已废弃 |
| 版本 | 所属物模型模板版本 |
| 备注 | 说明 |

## 6. 默认值设计

### 6.1 默认值定义

默认值是物模型在未被设备上报、未被用户配置或初始化创建时采用的标准值。默认值可以用于：

1. 设备首次绑定或激活后的初始化配置。
2. 云端设备影子或设备配置的初始值。
3. App、控制台展示的兜底值。
4. 批量配置、批量下发的基础值。
5. 产品类目模板复用时的推荐配置。

### 6.2 默认值分层

建议默认值支持四层：

| 层级 | 说明 | 示例 |
| --- | --- | --- |
| 系统默认值 | 平台基于数据类型提供的基础默认值 | Bool=false、Enum=第一个枚举项 |
| 类目默认值 | 产品类目模板中配置的默认值 | 哭声检测开关=开启 |
| 硬件默认值 | 不同硬件型号覆盖类目默认值 | 低配型号默认关闭高耗电检测 |
| 设备默认值 | 单个设备或设备分组覆盖默认值 | 某客户项目默认灵敏度=高 |

优先级建议：

设备默认值 > 硬件默认值 > 类目默认值 > 系统默认值。

### 6.3 不同物模型类型的默认值规则

#### 6.3.1 属性

属性是默认值配置的主要对象。

建议支持：

1. Bool：默认值为 true/false。
2. Enum：默认值必须从枚举项中选择。
3. Int/Float/Double：默认值必须在数值范围内，并符合步长。
4. Text：默认值长度不能超过数据定义限制。
5. Date：默认值支持固定时间或动态表达式，例如当前时间、设备激活时间。
6. Array：默认值需要符合元素类型和元素个数限制。
7. Struct：默认值需要逐个配置结构体子字段。

#### 6.3.2 服务

服务通常代表可调用动作，不建议简单配置“服务默认值”。但服务入参可以配置默认值，用于控制台调试、App 默认操作参数或接口调用模板。

建议支持：

1. 服务入参默认值。
2. 服务调用超时时间默认值。
3. 服务调用是否需要确认。
4. 服务出参不配置默认值，仅定义返回结构。

#### 6.3.3 事件

事件通常由设备上报，不建议配置事件默认值。但事件输出参数可以配置示例值或测试值，用于调试、文档和模拟上报。

建议区分：

1. 默认值：用于实际初始化或生效。
2. 示例值：用于文档、调试、模拟数据。

事件建议配置“示例值”，不建议配置“运行默认值”。

### 6.4 默认值表单规则

在添加/编辑物模型抽屉中，建议新增“默认值配置”区域。

字段建议：

| 字段 | 说明 |
| --- | --- |
| 是否启用默认值 | 开启后展示默认值输入控件 |
| 默认值 | 根据数据类型动态渲染输入方式 |
| 默认值来源 | 创建类目模板时默认为类目默认 |
| 是否允许硬件覆盖 | 控制硬件型号是否可以覆盖该默认值 |
| 是否允许设备覆盖 | 控制设备实例是否可以覆盖该默认值 |
| 空值策略 | 设备未上报时使用默认值、显示空值、显示未知 |
| 下发策略 | 仅作为展示兜底、设备激活时下发、每次恢复出厂后下发 |

### 6.5 默认值校验规则

1. 默认值必须符合数据类型。
2. 默认值必须符合数据定义。
3. Enum 默认值必须命中枚举项。
4. Bool 默认值只能为 true/false 或 0/1。
5. 数值默认值必须在最小值和最大值之间。
6. 数值默认值必须符合步长。
7. Struct 默认值必须按子字段类型递归校验。
8. Array 默认值必须符合元素类型和元素个数限制。
9. 只读属性允许配置展示默认值，但默认不支持设备下发。
10. 只写属性不建议配置展示默认值。

## 7. 优化后的核心流程

### 7.1 创建产品类目模板

```text
进入产品类目
 -> 新建产品类目
 -> 填写类目名称、类目编码、说明
 -> 创建类目物模型模板草稿
 -> 添加功能项分组
 -> 添加或引用物模型
 -> 配置默认值
 -> 校验模板
 -> 发布类目模板版本
```

### 7.2 添加物模型

```text
选择产品类目模板
 -> 点击添加物模型
 -> 选择属性/服务/事件
 -> 填写基础定义
 -> 配置数据定义
 -> 配置访问权限
 -> 配置默认值或示例值
 -> 保存为草稿
 -> 进入模板待发布变更列表
```

### 7.3 编辑物模型

```text
进入物模型列表
 -> 点击编辑
 -> 系统判断该物模型是否已发布
 -> 未发布：允许直接编辑
 -> 已发布：生成新草稿版本
 -> 修改定义或默认值
 -> 展示变更差异
 -> 校验兼容性
 -> 保存草稿
```

### 7.4 发布物模型模板

```text
进入模板草稿
 -> 查看本次变更
 -> 系统执行校验
 -> 展示影响范围
 -> 填写发布说明
 -> 确认发布
 -> 生成新版本
 -> 记录发布日志
 -> 支持回滚到上一版本
```

### 7.5 硬件型号覆盖默认值

```text
进入硬件型号
 -> 选择关联产品类目模板版本
 -> 继承类目物模型
 -> 查看可覆盖默认值字段
 -> 修改硬件默认值
 -> 校验
 -> 发布硬件适配配置
```

### 7.6 设备初始化应用默认值

```text
设备创建或激活
 -> 获取所属产品类目
 -> 获取硬件型号
 -> 计算最终默认值
 -> 写入设备配置/设备影子
 -> 按下发策略决定是否下发设备端
 -> 记录初始化结果
```

## 8. 页面优化建议

### 8.1 功能类目列表页

建议调整为“产品类目 / 物模型模板”管理页。

新增能力：

1. 展示当前类目的模板版本。
2. 展示已发布物模型数量、草稿变更数量。
3. 增加“物模型模板管理”入口。
4. 增加“发布记录”入口。
5. 功能项卡片展示绑定物模型数量和发布状态。

### 8.2 功能项详情页

建议弱化“功能项作为物模型容器”的概念，调整为：

1. 基础信息。
2. 绑定物模型。
3. 关联硬件。
4. 展示配置。
5. 发布记录。

物模型列表中建议新增字段：

| 字段 | 说明 |
| --- | --- |
| 默认值 | 展示当前类目默认值 |
| 默认值来源 | 类目/硬件/设备 |
| 状态 | 草稿、已发布、已废弃 |
| 版本 | 所属版本 |
| 来源 | 标准模板、自定义、硬件扩展 |

### 8.3 添加/编辑物模型抽屉

建议将表单分为四块：

1. 基础信息
   - 物模型名称、标识符、类型、描述。

2. 数据定义
   - 数据类型、枚举项、范围、单位、步长、结构体字段等。

3. 权限与默认值
   - 访问权限、默认值、空值策略、下发策略、是否允许覆盖。

4. 高级配置
   - 是否必填、是否展示、排序、兼容性策略、备注。

### 8.4 JSON 导入

JSON 导入需要支持默认值字段，并在导入后提供解析预览。

建议流程：

```text
粘贴 JSON
 -> 解析
 -> 展示新增/修改/冲突项
 -> 展示默认值校验结果
 -> 用户确认导入
 -> 保存为草稿
```

建议导入校验：

1. 标识符重复校验。
2. 数据类型合法性校验。
3. 默认值合法性校验。
4. 高风险变更提示。
5. 导入结果支持失败明细下载。

## 9. 数据结构建议

### 9.1 物模型定义示例

```json
{
  "name": "哭声检测灵敏度",
  "identifier": "cry_detection_sensitivity",
  "modelType": "property",
  "dataType": "enum",
  "accessMode": "rw",
  "required": false,
  "dataSpec": {
    "enumItems": [
      { "value": 0, "label": "高" },
      { "value": 1, "label": "中" },
      { "value": 2, "label": "低" }
    ]
  },
  "defaultConfig": {
    "enabled": true,
    "value": 1,
    "source": "category",
    "allowHardwareOverride": true,
    "allowDeviceOverride": true,
    "emptyValueStrategy": "use_default",
    "deliveryStrategy": "on_device_activation"
  },
  "description": "控制哭声识别的置信度阈值",
  "status": "draft",
  "version": "1.2.0"
}
```

### 9.2 结构体默认值示例

```json
{
  "name": "设置围栏区域",
  "identifier": "set_fence_area",
  "modelType": "property",
  "dataType": "array",
  "dataSpec": {
    "elementType": "struct",
    "minItems": 1,
    "maxItems": 512,
    "properties": [
      {
        "name": "横坐标",
        "identifier": "area_x",
        "dataType": "float",
        "min": 0,
        "max": 1,
        "step": 0.000001
      },
      {
        "name": "纵坐标",
        "identifier": "area_y",
        "dataType": "float",
        "min": 0,
        "max": 1,
        "step": 0.000001
      }
    ]
  },
  "defaultConfig": {
    "enabled": true,
    "value": [
      { "area_x": 0.1, "area_y": 0.1 },
      { "area_x": 0.9, "area_y": 0.9 }
    ],
    "source": "category",
    "allowHardwareOverride": true,
    "allowDeviceOverride": false,
    "emptyValueStrategy": "use_default",
    "deliveryStrategy": "manual_apply"
  }
}
```

## 10. 版本与发布机制

### 10.1 版本状态

建议模板版本包含以下状态：

| 状态 | 说明 |
| --- | --- |
| 草稿 | 可编辑，未对线上生效 |
| 待发布 | 已完成校验，等待确认发布 |
| 已发布 | 当前线上生效版本 |
| 已废弃 | 不再使用，但保留历史记录 |
| 已回滚 | 曾发布但被回滚 |

### 10.2 发布前校验

发布前必须校验：

1. 标识符唯一。
2. 必填字段完整。
3. 数据定义合法。
4. 默认值合法。
5. 服务入参/出参合法。
6. 事件输出参数合法。
7. 已发布字段的兼容性。
8. 关联硬件是否存在冲突覆盖。
9. 删除字段是否影响已接入设备。

### 10.3 高风险变更规则

以下变更应作为高风险变更：

1. 修改已发布物模型标识符。
2. 修改已发布物模型数据类型。
3. 删除已发布物模型。
4. 缩小数值范围导致已有默认值或设备值不合法。
5. 删除枚举项导致已有默认值失效。
6. 修改访问权限从读写变为只读或只写。
7. 修改结构体字段标识符或类型。

高风险变更建议处理方式：

1. 默认禁止直接修改。
2. 提供“废弃旧字段 + 新增字段”的推荐路径。
3. 如必须修改，需要二次确认并记录变更原因。

### 10.4 发布差异展示

发布确认页应展示：

1. 新增物模型数量。
2. 修改物模型数量。
3. 删除或废弃物模型数量。
4. 默认值变更数量。
5. 影响硬件型号数量。
6. 影响设备数量。
7. 高风险变更列表。

## 11. 权限与审计

建议增加以下权限点：

1. 产品类目创建/编辑/删除。
2. 物模型模板创建/编辑。
3. 默认值编辑。
4. 硬件默认值覆盖。
5. 设备默认值覆盖。
6. 物模型发布。
7. 物模型回滚。
8. JSON 导入。

审计日志应记录：

1. 操作人。
2. 操作时间。
3. 操作对象。
4. 操作类型。
5. 变更前内容。
6. 变更后内容。
7. 发布说明或变更原因。

## 12. 验收标准

### 12.1 默认值能力

1. 添加属性物模型时，可以根据数据类型配置默认值。
2. 编辑默认值时，系统能够校验默认值是否符合数据定义。
3. Enum 默认值只能选择已有枚举项。
4. 数值默认值必须符合范围与步长。
5. Struct 和 Array 默认值能够递归校验。
6. 服务入参支持默认值。
7. 事件参数支持示例值，不作为运行默认值。
8. 默认值支持类目、硬件、设备多层覆盖。
9. 系统能够按优先级计算最终默认值。

### 12.2 物模型管理

1. 用户可以在产品类目下统一维护物模型模板。
2. 功能项可以引用物模型，而不是强制重复创建物模型。
3. 已发布物模型修改后生成草稿变更。
4. 发布前可查看变更差异。
5. 高风险变更有明确提示或限制。
6. 支持查看历史版本。
7. 支持回滚到上一稳定版本。

### 12.3 JSON 导入

1. JSON 导入支持默认值字段。
2. 导入前展示解析预览。
3. 导入时校验标识符、数据类型、数据定义、默认值。
4. 导入失败时展示失败原因。
5. 导入成功后进入草稿状态，不直接发布。

### 12.4 发布闭环

1. 发布动作生成新的模板版本。
2. 发布记录可追溯。
3. 发布前展示影响范围。
4. 发布后当前版本状态更新为已发布。
5. 历史版本保留。
6. 支持回滚并记录回滚原因。

## 13. 实施优先级建议

### P0：补齐默认值与草稿发布闭环

1. 添加/编辑物模型增加默认值配置。
2. 增加默认值合法性校验。
3. JSON 导入支持默认值。
4. 物模型修改保存为草稿。
5. 发布前展示变更差异。

### P1：重构产品类目物模型模板

1. 产品类目下新增物模型模板管理。
2. 功能项改为引用物模型。
3. 支持模板版本管理。
4. 支持发布记录和回滚。

### P2：支持硬件和设备级覆盖

1. 硬件型号继承类目模板。
2. 硬件型号覆盖默认值。
3. 设备或设备分组覆盖默认值。
4. 支持最终默认值计算与展示。

### P3：标准物模型库与复用

1. 建立标准物模型库。
2. 支持从标准库引用物模型。
3. 支持跨类目复制模板。
4. 支持物模型废弃与迁移建议。

## 14. 结论

当前模块的问题本质不是缺少一个默认值字段，而是物模型作为 IoT 平台核心资产，没有形成独立、可复用、可版本化、可发布、可追溯的管理体系。

建议将当前“功能项详情中维护物模型”的模式，升级为“产品类目物模型模板 + 功能项引用 + 默认值分层覆盖 + 版本发布闭环”的模式。这样既能解决当前更新成本高、流程繁琐的问题，也能为后续硬件适配、设备初始化、批量配置、App 展示、标准物模型库建设打下基础。

## 15. 目标业务闭环详述

优化后的模块需要形成一条完整闭环，而不是只解决“编辑时多一个默认值输入框”。

完整闭环建议如下：

```text
标准能力沉淀
 -> 类目模板设计
 -> 功能项组织展示
 -> 物模型定义维护
 -> 默认值配置
 -> 草稿变更管理
 -> 发布校验
 -> 版本发布
 -> 硬件型号继承/覆盖
 -> 设备初始化/展示/下发
 -> 运行数据反哺
 -> 模板迭代
```

该闭环中每一层的责任应清晰拆分：

| 层级 | 核心责任 | 不应承担的责任 |
| --- | --- | --- |
| 产品类目 | 定义同类产品的标准能力模板 | 不直接承载单个设备差异 |
| 功能项 | 面向用户和业务的能力分组、展示组织 | 不作为物模型唯一生命周期容器 |
| 物模型 | 描述设备能力的结构化定义 | 不处理页面展示排序以外的业务 UI 逻辑 |
| 默认值 | 提供初始化、兜底展示、批量配置基础值 | 不替代设备真实上报值 |
| 硬件型号 | 继承类目模板并处理硬件差异 | 不反向污染类目标准模板 |
| 设备实例 | 应用最终配置并产生运行态数据 | 不修改模板定义 |

## 16. 核心业务对象关系

建议将对象关系调整为：

```text
产品类目 1 - N 物模型模板版本
物模型模板版本 1 - N 物模型定义
功能项 N - N 物模型定义
硬件型号 N - 1 产品类目
硬件型号 N - 1 模板版本
硬件型号 1 - N 默认值覆盖配置
设备实例 N - 1 硬件型号
设备实例 1 - N 设备级默认值覆盖配置
```

关键关系说明：

1. 产品类目是模板归属主体。
2. 模板版本是发布主体。
3. 物模型定义是可被功能项引用的能力资产。
4. 功能项是展示和业务分组，不再负责定义物模型生命周期。
5. 硬件型号继承某个模板版本，可覆盖允许覆盖的默认值。
6. 设备实例使用最终计算后的默认值，不直接修改类目模板。

## 17. 页面级需求说明

### 17.1 产品类目列表页

页面定位：从“功能类目列表”升级为“产品类目与模板管理入口”。

主要信息：

| 信息 | 说明 |
| --- | --- |
| 类目名称 | 产品类目名称 |
| 类目编码 | 全局唯一编码 |
| 当前模板版本 | 当前线上生效版本 |
| 模板状态 | 无模板、草稿中、已发布、存在未发布变更 |
| 物模型数量 | 当前版本下属性、服务、事件总数 |
| 草稿变更数 | 未发布的新增、修改、废弃数量 |
| 关联硬件数 | 使用该类目模板的硬件型号数量 |
| 最近发布时间 | 最近一次模板发布成功时间 |

主要操作：

1. 新增产品类目。
2. 编辑产品类目基础信息。
3. 进入物模型模板管理。
4. 查看发布记录。
5. 复制类目模板。
6. 停用产品类目。

交互要求：

1. 存在已发布模板的类目不允许直接删除，只允许停用。
2. 类目编码创建后不建议修改；如必须修改，需要校验下游引用。
3. 存在草稿变更时，应在列表中明确提示。

### 17.2 物模型模板管理页

页面定位：产品类目下的核心工作台。

建议页面结构：

```text
顶部：类目基础信息 + 当前版本 + 发布状态 + 发布按钮
左侧：功能项分组/物模型类型筛选
中部：物模型列表
右侧或抽屉：物模型详情、默认值、变更记录
底部或独立页：草稿变更列表、发布校验结果
```

物模型列表字段：

| 字段 | 说明 |
| --- | --- |
| 名称 | 物模型中文名称 |
| 标识符 | 唯一标识 |
| 类型 | 属性、服务、事件 |
| 数据类型 | Bool、Enum、Number、Struct、Array 等 |
| 访问权限 | 只读、读写、只写 |
| 默认值/示例值 | 属性展示默认值，服务入参展示默认值，事件展示示例值 |
| 绑定功能项 | 当前物模型被哪些功能项引用 |
| 变更状态 | 新增、已修改、已废弃、无变更 |
| 发布状态 | 草稿、已发布、已废弃 |

主要操作：

1. 新增物模型。
2. 编辑物模型。
3. 复制物模型。
4. 废弃物模型。
5. 绑定/解绑功能项。
6. JSON 导入。
7. JSON 导出。
8. 查看变更差异。
9. 发布模板。

### 17.3 新增/编辑物模型抽屉

新增/编辑抽屉建议按“基础定义 -> 数据定义 -> 默认值 -> 高级配置”组织。

基础定义：

| 字段 | 规则 |
| --- | --- |
| 物模型名称 | 必填，建议 1-50 字 |
| 标识符 | 必填，同一模板内唯一，只允许英文、数字、下划线 |
| 物模型类型 | 必填，属性/服务/事件 |
| 功能项绑定 | 可选，可绑定一个或多个功能项 |
| 备注 | 可选 |

数据定义：

| 类型 | 配置项 |
| --- | --- |
| Bool | true/false 展示文案 |
| Enum | 枚举值、枚举文案、排序 |
| Int/Float/Double | 最小值、最大值、步长、单位 |
| Text | 最大长度、格式约束 |
| Date | 时间格式、时区策略 |
| Array | 元素类型、最小元素数、最大元素数 |
| Struct | 子字段名称、标识符、类型、约束 |

默认值配置：

| 字段 | 规则 |
| --- | --- |
| 启用默认值 | 属性可启用；服务入参可启用；事件建议为示例值 |
| 默认值 | 根据数据类型动态渲染 |
| 空值策略 | 使用默认值、展示空值、展示未知 |
| 下发策略 | 不下发、设备激活下发、恢复出厂下发、手动应用 |
| 允许硬件覆盖 | 开启后硬件型号可覆盖 |
| 允许设备覆盖 | 开启后设备或设备分组可覆盖 |

高级配置：

| 字段 | 规则 |
| --- | --- |
| 是否必填 | 必填字段发布前必须有完整定义 |
| 是否展示 | 控制控制台/App 是否默认展示 |
| 排序 | 用于列表和功能项展示 |
| 兼容性策略 | 可新增、可废弃、禁止修改、允许扩展 |

### 17.4 草稿变更页

页面定位：让用户在发布前看清楚“这次到底改了什么”。

建议展示：

1. 新增物模型。
2. 修改物模型。
3. 废弃物模型。
4. 默认值变更。
5. 功能项绑定关系变更。
6. 高风险变更。
7. 影响硬件型号和设备数量。

差异展示规则：

| 变更类型 | 展示方式 |
| --- | --- |
| 新增字段 | 标记为新增，展示完整定义 |
| 删除字段 | 建议转为废弃，展示影响范围 |
| 修改名称/备注 | 普通变更 |
| 修改默认值 | 展示旧默认值和新默认值 |
| 修改数据类型 | 高风险变更 |
| 修改标识符 | 原则上禁止，建议新增字段替代 |

### 17.5 发布确认页

发布确认页至少需要包含：

1. 发布版本号。
2. 发布说明。
3. 变更摘要。
4. 校验结果。
5. 高风险变更确认。
6. 影响范围。
7. 是否同步到关联硬件型号。
8. 发布后是否触发默认值初始化任务。

发布按钮规则：

1. 校验失败时不可发布。
2. 存在高风险变更时需二次确认。
3. 存在硬件覆盖冲突时需先处理冲突。
4. 发布中需显示任务进度，避免重复点击。

## 18. 默认值应用策略

### 18.1 默认值计算规则

最终默认值建议按如下规则计算：

```text
if 设备级默认值存在且允许设备覆盖:
    使用设备级默认值
else if 硬件级默认值存在且允许硬件覆盖:
    使用硬件级默认值
else if 类目默认值存在:
    使用类目默认值
else:
    使用系统默认值或空值策略
```

需要注意：

1. 设备真实上报值优先于默认值。
2. 默认值不应覆盖设备已经上报的真实状态，除非用户明确执行重置或重新初始化。
3. 默认值下发应受访问权限和下发策略控制。
4. 只读属性默认值可用于展示兜底，但不应下发设备。
5. 只写属性默认值仅适合作为命令模板，不适合作为展示值。

### 18.2 默认值使用场景

| 场景 | 是否使用默认值 | 说明 |
| --- | --- | --- |
| 新设备创建 | 是 | 可写入设备配置或设备影子 |
| 设备首次激活 | 是 | 可按策略下发 |
| 设备未上报属性 | 是 | 可作为展示兜底 |
| 设备已上报属性 | 否 | 优先展示真实上报值 |
| 批量配置 | 是 | 作为批量任务的初始配置 |
| 恢复出厂设置 | 可选 | 取决于下发策略 |
| 服务调试 | 是 | 服务入参可带入默认值 |
| 事件模拟 | 否 | 使用示例值，不作为运行默认值 |

### 18.3 默认值冲突处理

可能出现的冲突：

1. 类目修改了默认值，但硬件型号已有覆盖值。
2. 修改了数据范围，导致硬件默认值非法。
3. 删除了枚举项，导致设备级默认值失效。
4. 禁用了允许覆盖，但下层已存在覆盖配置。

处理建议：

| 冲突类型 | 处理方式 |
| --- | --- |
| 下层覆盖仍合法 | 保留覆盖值，并提示不会自动继承类目新默认值 |
| 下层覆盖不合法 | 发布前阻断，要求修复或清空覆盖值 |
| 关闭允许覆盖 | 提示是否清理已有覆盖；未清理时不可发布 |
| 枚举项被删除 | 提示受影响默认值列表，要求替换为合法枚举 |

## 19. 数据权限与操作权限

建议权限拆分到可控粒度，避免所有人都能直接修改线上模板。

| 权限点 | 说明 |
| --- | --- |
| category:create | 创建产品类目 |
| category:update | 编辑产品类目 |
| model:create | 新增物模型 |
| model:update | 编辑物模型 |
| model:deprecate | 废弃物模型 |
| model:import | JSON 导入 |
| model:export | JSON 导出 |
| default:update | 编辑类目默认值 |
| hardware_default:update | 编辑硬件默认值 |
| device_default:update | 编辑设备默认值 |
| template:publish | 发布模板 |
| template:rollback | 回滚模板 |
| template:view_history | 查看历史版本 |

权限策略建议：

1. 普通产品人员可编辑草稿。
2. 发布权限应独立控制。
3. 高风险变更可要求管理员或负责人确认。
4. 回滚权限应高于普通发布权限。
5. JSON 导入权限应独立控制，因为导入可能批量影响大量字段。

## 20. 研发实现建议

### 20.1 表结构建议

建议至少拆分以下数据表或领域对象：

| 对象 | 说明 |
| --- | --- |
| product_category | 产品类目 |
| thing_model_template | 物模型模板 |
| thing_model_template_version | 模板版本 |
| thing_model_definition | 物模型定义 |
| thing_model_default_config | 默认值配置 |
| function_item | 功能项 |
| function_item_model_relation | 功能项与物模型绑定关系 |
| hardware_model | 硬件型号 |
| hardware_model_default_override | 硬件默认值覆盖 |
| device_default_override | 设备默认值覆盖 |
| thing_model_publish_record | 发布记录 |
| thing_model_change_record | 变更记录 |

### 20.2 API 能力建议

建议后端至少提供以下能力：

| API 能力 | 说明 |
| --- | --- |
| 查询类目模板 | 获取类目当前模板和草稿 |
| 保存草稿 | 新增或编辑物模型后保存草稿 |
| 校验草稿 | 执行类型、默认值、兼容性校验 |
| 查询差异 | 对比草稿和线上版本 |
| 发布模板 | 生成新版本并生效 |
| 回滚模板 | 回滚到历史稳定版本 |
| 查询最终默认值 | 按类目、硬件、设备计算最终默认值 |
| 导入 JSON | 解析并写入草稿 |
| 导出 JSON | 导出指定模板版本 |

### 20.3 兼容旧数据迁移

当前数据以功能项为主，迁移时建议采用渐进策略：

1. 将现有功能项下的物模型抽取为类目模板草稿。
2. 原功能项保留，自动生成与物模型的绑定关系。
3. 当前“待发布/未发布”状态映射为模板草稿状态。
4. 已发布功能项映射为模板初始版本 v1.0.0。
5. 旧物模型没有默认值时，默认值配置为未启用。
6. 对重复标识符进行迁移冲突标记，进入人工处理列表。

迁移后需要给用户一个确认页面，展示：

1. 成功迁移的类目数量。
2. 成功迁移的功能项数量。
3. 成功迁移的物模型数量。
4. 存在冲突的标识符。
5. 需要补充默认值的字段。

## 21. 边界场景与异常处理

### 21.1 标识符重复

同一模板版本内标识符必须唯一。导入或复制物模型时，如果标识符重复，应提示用户重命名或覆盖草稿中的同名字段。

不建议自动覆盖已发布字段，避免误删线上能力。

### 21.2 已发布物模型修改

已发布物模型被编辑时，应生成草稿变更，不直接修改线上版本。

允许直接修改的低风险字段：

1. 中文名称。
2. 描述。
3. 展示排序。
4. 默认值，前提是符合数据定义且不破坏下层覆盖。

需要限制的高风险字段：

1. 标识符。
2. 数据类型。
3. 服务入参结构。
4. 事件输出结构。
5. 数值范围收缩。
6. 枚举项删除。

### 21.3 删除物模型

不建议对已发布物模型执行物理删除。推荐使用“废弃”：

1. 废弃后新设备不再默认启用。
2. 历史设备仍可识别旧字段。
3. API 和数据解析链路保留兼容能力。
4. 发布记录中保留废弃原因。

### 21.4 默认值为空

默认值为空不一定是错误，需要结合策略判断：

| 场景 | 处理 |
| --- | --- |
| 未启用默认值 | 允许为空 |
| 启用默认值且字段必填 | 不允许为空 |
| Text 默认值为空字符串 | 允许，但需要与未配置区分 |
| Array 默认值为空数组 | 如果 minItems 为 0 则允许 |
| Struct 子字段为空 | 根据子字段必填规则判断 |

### 21.5 设备真实值与默认值不一致

默认值是初始化和兜底值，不是实时状态。页面展示时建议明确区分：

1. 当前值：设备真实上报值或用户配置值。
2. 默认值：模板或覆盖配置产生的默认值。
3. 值来源：设备上报、设备覆盖、硬件覆盖、类目默认、系统默认。

## 22. 指标与效果评估

优化完成后建议跟踪以下指标：

| 指标 | 目标 |
| --- | --- |
| 新建同类产品物模型耗时 | 明显下降 |
| 物模型重复录入次数 | 明显下降 |
| 默认值配置覆盖率 | 持续提升 |
| 发布失败率 | 通过发布前校验下降 |
| 高风险变更拦截次数 | 可被统计和追踪 |
| JSON 导入成功率 | 提升 |
| 设备初始化配置成功率 | 提升 |
| 因物模型定义不一致导致的问题数 | 下降 |

建议在上线后分阶段评估：

1. 第一阶段看配置效率和默认值合法性。
2. 第二阶段看模板复用和硬件覆盖效果。
3. 第三阶段看设备初始化、批量配置、App 展示的一致性。

## 23. 推荐迭代里程碑

### 23.1 第一阶段：默认值补齐

目标：在不大规模重构架构的情况下，先解决最急迫的默认值配置问题。

范围：

1. 添加/编辑物模型增加默认值配置。
2. 默认值按数据类型动态校验。
3. JSON 导入/导出支持默认值。
4. 物模型列表展示默认值。
5. 发布前校验默认值合法性。

### 23.2 第二阶段：草稿与版本机制

目标：解决已发布物模型修改不可控的问题。

范围：

1. 引入模板版本。
2. 已发布内容修改生成草稿。
3. 发布前展示差异。
4. 发布记录可追溯。
5. 支持历史版本查看和回滚。

### 23.3 第三阶段：类目模板化

目标：从根上解决功能项承担过多职责的问题。

范围：

1. 产品类目成为物模型模板归属主体。
2. 功能项改为引用物模型。
3. 支持类目模板复制。
4. 支持标准物模型沉淀。

### 23.4 第四阶段：硬件与设备覆盖

目标：支持真实 IoT 场景中的型号差异和项目差异。

范围：

1. 硬件型号继承类目模板。
2. 硬件默认值覆盖。
3. 设备或设备分组默认值覆盖。
4. 最终默认值计算与展示。
5. 设备初始化任务记录。

## 24. 待确认问题

为了进入研发评审，还需要业务和技术进一步确认以下问题：

1. 当前“产品类目”是否等同于真实产品品类，还是仅为功能分组。
2. 当前“功能项”是否需要继续作为用户可见的一级对象。
3. 一个物模型是否允许绑定多个功能项。
4. 硬件型号与产品类目的关系是一对一还是多对多。
5. 默认值是否需要真正下发到设备，还是只用于云端/App 展示。
6. 哪些属性是可写配置，哪些属性只是只读状态。
7. 设备影子或配置中心是否已经存在，可否承接默认值初始化。
8. 当前发布动作是否有审批流要求。
9. 历史设备是否需要兼容旧物模型标识符。
10. JSON 导入格式是否需要兼容阿里云、华为云或内部已有 TSL 格式。

## 25. 产品决策建议

综合现状和目标，建议产品方案采用“先补能力、再重构架构”的路线：

1. 短期不要只做一个默认值字段，否则后续仍会被版本、复用、发布、硬件差异反复牵制。
2. 第一版可以先在当前功能项详情页内增加默认值与草稿校验能力，但数据模型要按类目模板预留。
3. 中期应把物模型从功能项中抽离出来，让功能项变成引用关系。
4. 发布主体应从功能项升级为物模型模板版本。
5. 默认值必须从第一天就支持类型校验和来源标识，否则后续硬件覆盖、设备覆盖会难以补齐。
6. 对已发布物模型，应优先采用“废弃旧字段 + 新增字段”的兼容策略，减少线上设备解析风险。

最终推荐方案：

```text
产品类目作为模板主体
功能项作为展示分组
物模型作为独立资产
默认值作为模板能力的一部分
版本发布作为变更边界
硬件/设备覆盖作为差异化配置
```

该方案既能覆盖当前“默认值配置”和“更新流程繁琐”的直接诉求，也符合标准物联网平台对物模型的长期演进要求。
