# IoT Thing Model Admin

物模型默认值优化原型项目，基于 Vue 3、Vite 和 Element Plus 构建。

## 本地开发

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
```

## GitHub Pages 部署

推送到 `main` 分支后，GitHub Actions 会自动构建并部署 `dist` 到 GitHub Pages。
